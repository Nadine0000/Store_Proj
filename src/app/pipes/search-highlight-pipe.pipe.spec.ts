import { SearchHighlightPipePipe } from './search-highlight-pipe.pipe';

describe('SearchHighlightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHighlightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
