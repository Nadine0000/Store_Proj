import { ForstyleDirective } from './forstyle.directive';

describe('ForstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ForstyleDirective();
    expect(directive).toBeTruthy();
  });
});
